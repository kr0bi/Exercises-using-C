{
  "version": "2.0.0",
  "tasks": [
      {
          "label": "execute current program",
          "type": "shell",
          "command": "g++",
          "args": [
            "-g",
            "-lm",
            "${file}",
            "-o",
            "${fileDirname}/${fileBasenameNoExtension}"
          ],
          "group": {
              "kind": "build",
              "isDefault": true
          }
      }
  ]
}